{
  "version": 2,
  "buildCommand": "cd design-system-storybook && rm -rf node_modules && npm install && npm run build-storybook",
  "outputDirectory": "design-system-storybook/storybook-static",
  "installCommand": "echo 'skipping root install'",
  "cleanUrls": true,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
